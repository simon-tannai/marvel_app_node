"use strict";const log4js=require("log4js"),path=require("path"),fs=require("fs");let logger;if("production"===process.env.NODE_ENV){const e=path.join(__dirname,"..","config","log4js.json"),r=require(e);fs.existsSync(path.dirname(r.appenders[0].filename))||fs.mkdirSync(path.dirname(r.appenders[0].filename)),log4js.configure(e),logger=log4js.getLogger("log")}else logger=log4js.getLogger();module.exports=logger;